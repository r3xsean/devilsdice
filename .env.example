# ===========================================
# Devil's Dice - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values

# ----- Redis Configuration -----
# Connection URL for Redis (used for session storage and pub/sub)
REDIS_URL=redis://localhost:6379

# ----- Server Configuration -----
# Port for the NestJS server
PORT=3001

# Environment (development, production, test)
NODE_ENV=development

# CORS allowed origins (comma-separated for multiple origins)
# Example: http://localhost:5173,https://myapp.com
CORS_ORIGIN=http://localhost:5173

# ----- Client Configuration -----
# WebSocket server URL (used by the client to connect)
# For local development: http://localhost:3001
# For production: https://api.your-domain.com
VITE_WS_URL=http://localhost:3001

# Base path for the client (default: /)
# Use this if deploying to a subdirectory
VITE_BASE_PATH=/

# ----- Production Deployment -----
# These are typically set in your deployment platform (Vercel, Fly.io, etc.)

# For Fly.io deployment:
# FLY_APP_NAME=your-app-name

# For Vercel deployment:
# VERCEL_URL is automatically set by Vercel
