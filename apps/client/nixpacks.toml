[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["cd ../.. && pnpm install"]

[phases.build]
cmds = ["cd ../.. && pnpm --filter @devilsdice/shared build && pnpm --filter @devilsdice/client build"]

[start]
cmd = "npx serve dist -s -l 3000"
